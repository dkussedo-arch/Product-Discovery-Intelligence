# Anthropic (required for synthesis / RAG queries)
ANTHROPIC_API_KEY=

# Optional: default API base for external clients
NEXT_PUBLIC_PDI_API_URL=

# Optional: extra CORS origins (comma-separated) for AI Studio / custom frontends
ALLOWED_CORS_ORIGINS=

# Optional: Google AI Studio / Gemini (for UI-only flows in AI Studio prototype)
GOOGLE_GENERATIVE_AI_API_KEY=
